# Only the gatekeeper requires Hussein's review — everything else is freely modifiable.
# Combined with branch protection on main, no PR touching gatekeeper.py can merge
# without Hussein's explicit approval, regardless of who or what opens it.

core/gatekeeper.py    @HusseinShaib
