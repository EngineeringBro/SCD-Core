name: SCD Core Agent

on:
  schedule:
    # Runs every 10 minutes
    - cron: '*/10 * * * *'
  workflow_dispatch:
    # Allows manual trigger from GitHub Actions UI

jobs:
  run-agent:
    name: Process SCD Queue
    runs-on: ubuntu-latest
    timeout-minutes: 8

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Create logs directory
        run: mkdir -p logs

      - name: Run SCD Core Agent
        env:
          JIRA_EMAIL: ${{ secrets.JIRA_EMAIL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          # LIVE_MODE is intentionally NOT set here.
          # The agent defaults to DRY_RUN unless LIVE_MODE=true is explicitly
          # added as a GitHub Secret by a repo admin. This prevents accidental live execution.
          LIVE_MODE: ${{ secrets.LIVE_MODE }}
        run: python scd_agent.py

      - name: Upload audit log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: audit-log-${{ github.run_number }}
          path: logs/
          retention-days: 30
